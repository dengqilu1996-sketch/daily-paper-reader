---
title: Autoencoder Architectures for Athlete Performance Scoring from Wearable Telemetry
title_zh: 基于可穿戴遥测的运动员表现评分自编码器架构
authors: "Mateusz Kubita, Jan Zubalewicz, Krzysztof Siwek"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.28145v1"
tags: ["query:ml-sports"]
score: 9.0
evidence: 自编码器将可穿戴跑步数据压缩为标量性能得分
tldr: 可穿戴设备生成大量高维训练日志，解释成为瓶颈。本文比较五种降维模型，通过重建误差与可解释性复合标准，将九维传感器数据压缩为一个性能评分。深度自编码器复合得分最高，加宽PCA隐藏层后深层变体性能相近，表明容量而非一维瓶颈是限制因素。跑步配速、有氧解耦和平均心率是主要驱动特征，复合标准避免了仅依赖重建精度选模。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28145v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 916, \"height\": 541, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28145v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 850, \"height\": 544, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28145v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 917, \"height\": 797, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28145v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1040, \"height\": 260, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28145v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 644, \"height\": 259, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28145v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 776, \"height\": 163, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28145v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 941, \"height\": 291, \"label\": \"Table\"}]"
motivation: 面对可穿戴训练日志解释困难，需无监督地从多传感器数据中提取单一可解释的性能指标。
method: 评估三种自编码器、PCA、VAE，使用MSE和四个可解释性指标（秩相关、互信息、置换重要性）构建复合标准，结合Borda计数和引导验证。
result: 深度自编码器实现最低重建误差和最高复合分数；PCA加宽后深层变体竞争力提升；关键特征为配速、有氧解耦、平均心率。
conclusion: 模型容量而非潜在瓶颈是关键限制；复合选择标准可有效评估无监督降维模型的准确性和可解释性，捕捉符合生理学的指标。
---

## 摘要
可穿戴设备为日常跑者生成大量高维训练日志，当前瓶颈在于解读而非数据收集。本文评估了五种降维模型——三种自编码器变体、主成分分析和变分自编码器，考察其将九个传感器跑者画像压缩为单一标量表现指标（潜在得分）的能力。由于完全无监督，模型质量沿两个互补轴评价：重建误差（均方误差）和潜在得分可解释性，后者通过斯皮尔曼与肯德尔秩相关、互信息及排列重要性测量。这些指标合并为综合选择标准，避免仅凭重建精度选择模型。四个指标的特征排名通过修正的波达计数法汇总，并通过自助验证确认稳定性。双特征线性基线作为比较基准。深层自编码器取得了最低的重建误差和最高的综合得分。当主成分分析的隐藏层被拓宽后，更深层变体在综合标准上与深层自编码器紧密竞争，表明限制因素是隐藏层容量而非一维瓶颈。跨所有模型和重采样运行，跑步配速、有氧解耦和平均心率成为主导潜在得分的因子，与已有生理学认知一致。

## Abstract
Wearable devices produce large, high dimensional training logs for everyday runners, and interpretation rather than data collection is now the limiting step. This paper evaluates five dimensionality reduction models, three autoencoder variants, PCA, and a Variational Autoencoder, on their ability to compress nine sensor runner profiles into a single scalar performance indicator, the latent score. Because the setting is fully unsupervised, model quality is assessed along two complementary axes: reconstruction error (Mean Squared Error) and latent score interpretability, measured via Spearman and Kendall rank correlations, Mutual Information, and Permutation Importance. These are combined into a composite selection criterion that prevents selecting models on reconstruction accuracy alone. Feature rankings from the four metrics are aggregated via a modified Borda count, and their stability is confirmed by bootstrap validation. A two feature linear baseline is included to anchor the comparison. Deep autoencoder achieved the lowest reconstruction error and the highest composite score. Once the PCA hidden layers were widened, the deeper variants became closely competitive with Deep AE on the composite criterion, indicating that the limiting factor was hidden layer capacity rather than the one dimensional bottleneck. Running pace, aerobic decoupling, and average heart rate emerged as the dominant latent score drivers across all models and resampling runs, consistent with established physiology.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义
- 可穿戴设备日常生成大量高维训练日志（心率、步频、配速等），数据解读已成瓶颈，教练需要将多维度传感器信息压缩为单一、可解释的性能评分。
- 现有无监督降维方法多仅优化重建误差，忽略了评分是否可解释、跨数据划分是否稳定，且线性方法难以捕捉非线性生理关系。
- 本文旨在构建一个统一评估框架，实现将9维跑步者遥测数据压缩为一个潜在得分，并在模型筛选时同时考虑重建精度和排序可解释性，为无监督运动员排名提供可复现、可解释的方案。

## 2. 方法论
- **模型架构**
  - 对比五种降维模型：简单自编码器（Simple AE）、中等自编码器（Medium AE，输入→16→1→16→输出）、深度自编码器（Deep AE，32→8→1→8→32）、主成分分析（PCA）和变分自编码器（VAE，中间维度32）。
  - 所有模型的瓶颈固定为一维（潜在得分），周围隐藏层保持较宽，以研究容量限制。
- **复合选择标准**
  - 定义得分：`Score_sel = α·(1 − MSE_norm) + (1−α)·Q`，其中MSE_norm为各模型最小-最大归一化后的测试集MSE，Q为潜在得分质量（见下），α=0.5（默认）。
  - 该标准避免单纯按重建误差选模，平衡保真度与可解释性。
- **潜在得分质量 Q 与特征解释**
  - 采用四个指标量化特征与潜在得分的关系：斯皮尔曼秩相关系数、肯德尔秩相关系数、互信息、置换重要性（基于随机森林回归，80棵树）。
  - 每个指标生成特征排名，通过修正的波达计数法聚合为“综合影响得分”：
    - 归一化得分：`s_{m,f} = 1 − (r_{m,f} − 1)/(p − 1)`，p为特征总数。
    - 最终得分：`S_f = Σ w_m·s_{m,f}`，权重默认为0.25/0.20/0.25/0.30（Spearman/Kendall/MI/Permutation）。Q定义为排名最高特征在四指标上的平均归一化综合影响得分。
  - 权重敏感性通过狄利克雷分布采样1000组权重进行评估。
- **稳定性验证**
  - 自助抽样（bootstrap）计算特征排名分布，报告Top-3包含概率、中位数排名和95%置信区间。
- **线性基线**
  - `Score_base = 0.5·pace + 0.5·HR`（标准化后），作为最简手工组合的参考。

## 3. 实验设计
- **数据集**：来自Golden Cheetah平台的导出数据（`activities.csv`），预处理后共45,836条观测，80/10/10训练/验证/测试划分。每条数据包含9个特征：配速（pace_min_km）、平均心率（average_hr）、海拔增益（elevation_gain）、总距离（total_distance）、步频（final_cadence）、有氧解耦（aerobic_decoupling）、年龄（age）、体重（athlete_weight）、性别（binary, is_male）。所有特征经Min-Max归一化。
- **基准**：简单AE、PCA作为线性基线；VAE作为概率潜在空间代表。
- **训练与评估**：神经网络模型在验证集上调优早停（patience=10 epoch），每种模型独立训练5次（随机种子）取平均指标；PCA仅拟合一次。最终指标在测试集上计算。复合标准中α还测试了0.3和0.7的稳健性。

## 4. 资源与算力
- 文中未提及任何GPU型号、数量、训练时长或具体硬件配置。仅指出每个神经网络模型使用不同随机种子训练5次，但算力细节缺失。

## 5. 实验数量与充分性
- 共评估5种架构，每种神经网络进行5次随机初始化实验。
- 复合标准进行了α权重敏感性分析（α=0.3,0.5,0.7）；特征权重敏感性用1000组狄利克雷采样验证。
- 特征排名稳定性通过自助法bootstrap给出置信区间。
- 包含与线性基线的比较和模型间潜在得分相关性分析。
- 实验在同一数据集上完成，未进行跨平台泛化验证，但总体设计细致，涵盖重建误差、可解释性、稳定性、容量影响等多个维度，实验较为充分、公平。

## 6. 主要结论与发现
- Deep AE获得最低测试MSE（0.001781）和最高复合得分（0.972），其次为PCA（0.927）、VAE（0.915）、Medium AE（0.892）、Simple AE（0.407）。
- 加宽隐藏层后，Medium AE与VAE、PCA的差距显著缩小，表明瓶颈宽度不是主要限制，而是编码器容量。
- 跨所有模型和重采样，跑步配速、有氧解耦、平均心率是最稳定的Top-3特征（Top-3包含概率1.000），符合运动生理学认知。
- 简单按MSE选模可能误选，例如PCA与Simple AE潜在评分与运动学解读相悖（两模型间Spearman ρ = −0.65），复合标准可有效规避。
- 与双特征线性基线对比，Deep AE能额外捕捉有氧解耦等非线性信息，评分不可互换。

## 7. 优点
- 提出一个完整无监督框架，将多维遥测压缩为单一可解释得分，并创新性地采用复合标准兼顾重建精度和可解释性。
- 四种可解释性指标与波达计数法的多视角共识评估，降低单一指标的片面性，并通过bootstrap验证稳定性。
- 容量 vs. 瓶颈的分析揭示深层模型价值，为架构设计提供指导。
- 识别出的关键特征与运动生理学高度一致，无需任何标注数据或领域先验。
- 权重敏感性分析表明结论对α选择不敏感，稳健性较好。

## 8. 不足与局限
- **数据局限性**：仅来自单一训练平台（Golden Cheetah），未在Garmin、Polar等其他设备生态验证，外部有效性受限。
- **缺乏真值标签**：无VO₂max、比赛成绩等金标准，潜在得分的有效性仅通过表面效度和生理学常识佐证。
- **性别处理简化**：性别以二元变量建模，未考虑激素或解剖差异。
- **时序信息丢失**：使用会话级聚合特征，忽略训练过程中的动态疲劳变化，未来需序列模型（LSTM/Transformer）。
- **权重选择的开放性**：默认α=0.5为约定选择，且特征权重虽做敏感性分析，但前3特征集的精确顺序保持概率较低（6.8%），权重仍有影响。
- **算力报告缺失**：未说明训练所需的实际计算资源。

（完）
