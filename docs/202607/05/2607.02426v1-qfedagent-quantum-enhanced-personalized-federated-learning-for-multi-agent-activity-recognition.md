---
title: "QFedAgent: Quantum-Enhanced Personalized Federated Learning for Multi-Agent Activity Recognition"
title_zh: QFedAgent：面向多智能体活动识别的量子增强个性化联邦学习
authors: "Quoc Bao Phan, Tuy Tan Nguyen"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02426v1"
tags: ["query:cdm-sports"]
score: 9.0
evidence: 量子增强的个性化联邦学习用于多模态传感器活动识别
tldr: "多智能体活动识别需保护隐私，但联邦学习中非IID多模态数据和经典融合模块参数开销大。本文提出混合量子-经典个性化联邦学习框架QFedAgent，其核心采用变分量子电路融合加速度计与陀螺仪，仅需72参数。在OPPORTUNITY数据集上非IID划分下，QFedAgent获97.7%平均测试精度，参数量比MLP融合降低约10倍。该框架证明量子融合在资源受限联邦学习中的高效性与竞争力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02426v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1230, \"height\": 328, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02426v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 893, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02426v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 760, \"height\": 713, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02426v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 855, \"height\": 635, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02426v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 937, \"height\": 323, \"label\": \"Table\"}]"
motivation: 现有联邦学习在多主体活动识别中，因数据非IID和经典融合模块参数巨大，导致性能下降和通信负担。
method: 提出QFedAgent框架，采用变分量子电路作为融合模块，利用量子态编码和纠缠捕获传感器交互，仅需72参数。
result: "在OPPORTUNITY数据集非IID划分下，QFedAgent达到97.7%平均测试精度，参数量比经典MLP融合减少约10倍。"
conclusion: QFedAgent验证了量子融合在个性化联邦活动识别中的参数效率与竞争力，为资源受限多智能体系统提供新思路。
---

## 摘要
联邦学习（FL）允许分布式设备在不共享原始数据的情况下协作训练模型，使其适用于隐私敏感的机器人感知应用。然而，多智能体系统产生的异构且非独立同分布（non-IID）多模态传感器数据流会降低传统FL算法的性能，而经典融合模块则带来大量参数开销和通信成本。本文提出QFedAgent，一种面向多智能体活动识别的混合量子-经典个性化联邦学习框架。该方法集成了一个变分量子电路融合模块，通过量子态编码和纠缠对加速度计-陀螺仪相互作用进行建模，仅需72个量子旋转参数，而基于经典多层感知器的融合需要33K参数，实现了约10倍的参数总量减少。在基于受试者的非IID划分的OPPORTUNITY数据集上进行的实验表明，平均测试准确率达到97.7%，证实了参数高效的量子融合与传统联邦基线相比仍具有竞争力。

## Abstract
Federated learning (FL) enables collaborative model training across distributed devices without sharing raw data, making it suitable for privacy-sensitive robotic sensing applications. However, multi-agent systems generate heterogeneous and non-independent and identically distributed (non-IID) multimodal sensor streams that degrade conventional FL algorithms, while classical fusion modules introduce substantial parameter overhead and communication cost. This paper proposes QFedAgent, a hybrid quantum-classical personalized FL framework for multi-agent activity recognition. The approach integrates a variational quantum circuit fusion module that models accelerometer--gyroscope interactions through quantum state encoding and entanglement, requiring only 72 quantum rotation parameters versus 33K in classical multi-layer perceptron-based fusion, achieving approximately 10x total parameter reduction. Experiments on the OPPORTUNITY dataset under subject-based non-IID partitions demonstrate 97.7% mean test accuracy, confirming that parameter-efficient quantum fusion remains competitive with conventional federated baselines.

---

## 论文详细总结（自动生成）

好的，以下是基于您提供的论文内容生成的结构化中文总结。

### 1. 论文的核心问题与整体含义

*   **研究动机与背景**：
    *   自主机器人系统在工业自动化、医疗监测等领域日益普及，它们产生大量多模态传感器数据（如IMU数据），但将这些数据集中训练存在通信开销、延迟和隐私问题。
    *   联邦学习（FL）为分布式协作训练提供了解决方案，允许本地数据不出设备。
    *   **核心问题**：在多智能体活动识别任务中，面临两大挑战：
        1.  **数据异构性**：不同智能体因环境和运动模式差异，产生非独立同分布（non-IID）数据，导致传统FL算法（如FedAvg）性能下降。
        2.  **参数开销大**：处理加速度计和陀螺仪等多模态信号时，经典的融合方法（如注意力机制、MLP）往往引入大量参数，增加通信成本，加剧异构客户端间的聚合难题。
*   **整体含义**：本文旨在提出一种**参数高效**的个性化联邦学习框架，利用**变分量子电路（VQC）** 替代经典融合模块，以应对多模态数据融合的挑战，在保持高精度的同时大幅减少模型参数，验证了量子融合在资源受限的联邦学习环境中的潜力与竞争力。

### 2. 论文提出的方法论

*   **核心思想**：提出 **QFedAgent**，一个混合量子-经典的个性化联邦学习框架。其核心创新在于用**变分量子电路（VQC）作为参数高效的多模态融合模块**，同时对联邦学习系统进行个性化设计以应对数据异构。
*   **关键技术与架构**：
    *   **模型架构**：包含共享的全局参数和私有的本地参数。
        *   **全局参数** $w_g$：双CNN编码器（提取加速度计、陀螺仪特征），VQC融合层（核心），投影层。这些参数由服务器聚合。
        *   **本地参数** $w_l^k$：客户端私有的适配器（轻量级，捕捉客户端特性）和分类头。这些参数不上传，实现个性化。
    *   **VQC融合层细节**（参数估计约72个量子旋转参数）：
        1.  **输入变换**：将两个CNN编码器输出的模态嵌入向量 $e_a, e_g$ 拼接，通过经典线性层映射为量子比特旋转角度 $\phi$。
        2.  **量子态编码与纠缠**：将角度 $\phi$ 通过RX旋转门编码到 $N=8$ 个量子比特的量子态上。其中，加速度计嵌入编码到Qubit 0-3，陀螺仪嵌入编码到Qubit 4-7。然后通过 $L=3$ 层强纠缠层（StronglyEntanglingLayers）实现量子纠缠，以捕获加速度计和陀螺仪之间的跨模态关联。
        3.  **测量与输出**：测量每个量子比特的Pauli-Z期望值，得到融合表示 $z \in [-1,1]^N$。最后通过一个经典线性层和层归一化将其映射回嵌入空间，得到最终的融合嵌入。该层输出有界且李普希茨连续，利于梯度优化。
    *   **联邦训练协议**：
        *   流程（算法1）：服务器广播全局参数 $w_g$ → 客户端本地训练一个epoch（同时更新全局和本地参数） → 客户端仅上传更新后的全局参数 → 服务器使用加权FedAvg（按数据量加权）聚合。
        *   VQC梯度利用参数偏移法则（parameter-shift rule）进行无偏估计，可与经典反向传播兼容。

### 3. 实验设计

*   **数据集与场景**：在 **OPPORTUNITY Activity Recognition 数据集** 上进行评估。该数据集包含4名受试者（S1-S4）执行日常活动的IMU记录。每个受试者作为一个FL客户端，天然构成基于受试者的非IID数据划分。
*   **任务与数据处理**：使用15通道的加速度计和陀螺仪信号，分割为时长64个时间步、50%重叠的窗口。任务是识别4类运动状态：站立、行走、坐着、躺着。全局测试集从所有窗口按20%比例分层抽样构建。
*   **对比方法（基准）**：
    1.  **Local only**：仅用本地数据训练，不进行联邦。
    2.  **FedAvg** [9]：经典联邦平均算法。
    3.  **FedProx** [10]：用于处理异构性的联邦优化算法。
    4.  **MLP-FL**：在与QFedAgent相同的个性化FL协议下，将VQC融合层替换为经典MLP融合层的模型。这是与QFedAgent参数效率对比的关键基准。

### 4. 资源与算力

*   **明确信息**：论文明确指出，实验使用 **NVIDIA RTX 5080 GPU** 进行训练。
*   **训练时间对比**：文中在表I中报告了**每轮训练时间**：
    *   经典方法（Local, FedAvg等）约为 9.8 - 12.5 秒。
    *   **QFedAgent 为 121.4 秒**，是经典方法的约10倍。论文解释此开销源于在经典硬件上模拟量子电路，需要跟踪完整的 $2^N$ 维量子态并为每个参数执行两次电路评估来计算梯度。并指出此开销在真实量子硬件上将会消失。

### 5. 实验数量与充分性

*   **大约实验组数**：实验设计相对集中，主要进行了以下几组关键对比与分析，足以支撑其核心论点：
    1.  **方法对比实验**：在主要任务上对比了1个本地方法与4个联邦学习/融合方法（Local, FedAvg, FedProx, MLP-FL, QFedAgent），报告了收敛损失曲线和最终测试准确率。
    2.  **详细性能剖析**：表I提供了各方法融合参数量、训练时间和每个客户端（S1-S4）的测试准确率明细。
    3.  **消融研究**：对VQC的电路配置（量子比特数 $N$ 和纠缠层数 $L$）进行了消融实验，探索其对准确率的影响。
*   **实验的充分性、客观性与公平性**：
    *   **客观且公平**：所有方法共享相同的CNN编码器和分类器架构，仅改变融合层（ VQC vs. MLP）或训练协议（本地 vs. 联邦聚合法），对比公平。使用标准的公开数据集和非IID划分，结果可信。
    *   **充分性适中**：实验有效证明了VQC融合的参数高效性和准确性优势。但实验仅在一个活动识别数据集（OPPORTUNITY）和一种非IID划分（按受试者）上进行，结论的泛化性有待更多数据集和更复杂非IID设定（如标签分布倾斜）的验证。

### 6. 论文的主要结论与发现

1.  **性能领先**：QFedAgent在OPPORTUNITY数据集上取得了**97.7%的平均测试准确率**，超越了FedAvg、FedProx和MLP-FL等所有对比方法。
2.  **参数高效性显著**：VQC融合模块的量子旋转参数仅为**72个**，而经典的MLP融合有33K参数，实现了大约 **10倍的参数总量缩减**。这表明量子纠缠能非常高效地建模跨模态交互。
3.  **联邦训练稳定有效**：尽管数据是非IID的，QFedAgent的损失收敛曲线平滑且最终损失最低，验证了量子参数化模块在联邦优化中的稳定性。
4.  **架构有效性**：消融实验表明，适当宽度和深度的VQC足以提供必要的表达能力，更深更宽的电路可能带来优化困难。
5.  **当前代价与未来潜力**：主要缺点是经典模拟量子电路导致训练时间较长，但这被认为是是模拟器本身的开销，而非算法固有问题，未来在真实量子硬件上执行将不再有此瓶颈。

### 7. 优点

*   **创新性强**：创新地将变分量子电路应用到个性化联邦学习的多模态融合环节，连接了两个前沿领域，为解决参数开销问题提供了新视角。
*   **参数效率极高**：实验清晰地展示了VQC以极少的参数量达成甚至超越大型经典网络的性能，这对于通信带宽有限、硬件资源受限的边缘设备极具价值。
*   **个性化的联邦学习设计**：通过分离全局共享部分（编码器、VQC融合）和客户端私有部分（适配器、分类头），自然地平衡了全局知识共享与本地个性化，贴合多智能体系统的异构特性。
*   **实验对比清晰**：直接与MLP-FL在完全相同的联邦设置下对比，有力地证明了性能提升来源于量子融合本身，而非其他架构差异。

### 8. 不足与局限

*   **实验覆盖范围有限**：
    *   仅在**一个数据集**（OPPORTUNITY）和单一种类的非IID划分（基于受试者）上验证，模型的泛化能力未在其他数据集或多模态任务上得到证明。
    *   缺乏对**通信代价**的实际评估，例如报告每轮通信传输的参数量和数据量，虽然逻辑上参数少通信量低，但缺少量化对比。
*   **模拟效率瓶颈**：在经典硬件上模拟VQC导致训练速度慢，这限制了其在当前阶段直接应用于对实时性要求高、无量子硬件访问的场景。
*   **量子优势的理论深度不足**：论文展示了VQC的参数效率，但未深入分析或理论证明为何量子纠缠在此特定任务中能更好地建模加速度计-陀螺仪的相关性，其“黑盒”性质仍存。
*   **框架的超参数依赖性**：如VQC的比特数、层数等需要人为设定并进行消融实验找到较优值，可能存在其他更优的量子电路架构（ansatz）未被探索。

（完）
